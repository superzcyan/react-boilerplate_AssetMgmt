/*
 * AssetMgmtFilterSort Messages
 *
 * This contains all the text for the AssetMgmtFilterSort component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AssetMgmtFilterSort';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AssetMgmtFilterSort component!',
  },
});
