/*
 * SideNavBar Messages
 *
 * This contains all the text for the SideNavBar component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SideNavBar';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Asset Management',
  },
});
