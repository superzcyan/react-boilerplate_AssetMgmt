/*
 * AssetAddAsset Messages
 *
 * This contains all the text for the AssetAddAsset container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AssetAddAsset';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AssetAddAsset container!',
  },
});
