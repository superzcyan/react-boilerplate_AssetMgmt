// import { selectAssetSuppliersDomain } from '../selectors';

describe('selectAssetSuppliersDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
