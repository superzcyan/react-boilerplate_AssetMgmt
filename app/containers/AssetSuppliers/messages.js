/*
 * AssetSuppliers Messages
 *
 * This contains all the text for the AssetSuppliers container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AssetSuppliers';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AssetSuppliers container!',
  },
});
