/*
 * AssetVideoCard Messages
 *
 * This contains all the text for the AssetVideoCard container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AssetVideoCard';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AssetVideoCard container!',
  },
});
