// import { selectAssetCategoriesDomain } from '../selectors';

describe('selectAssetCategoriesDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
