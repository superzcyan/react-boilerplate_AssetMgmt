// import { selectAssetProcessorsDomain } from '../selectors';

describe('selectAssetProcessorsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
