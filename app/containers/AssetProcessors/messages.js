/*
 * AssetProcessors Messages
 *
 * This contains all the text for the AssetProcessors container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AssetProcessors';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AssetProcessors container!',
  },
});
