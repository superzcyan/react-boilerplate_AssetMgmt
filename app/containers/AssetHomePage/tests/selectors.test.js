// import { selectAssetHomePageDomain } from '../selectors';

describe('selectAssetHomePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
