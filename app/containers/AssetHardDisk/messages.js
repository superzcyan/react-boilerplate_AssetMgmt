/*
 * AssetHardDisk Messages
 *
 * This contains all the text for the AssetHardDisk container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AssetHardDisk';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AssetHardDisk container!',
  },
});
