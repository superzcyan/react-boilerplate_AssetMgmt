// import { selectAssetManufacturersDomain } from '../selectors';

describe('selectAssetManufacturersDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
